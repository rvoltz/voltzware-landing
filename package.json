{
  "name": "voltzware-landing",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/ && cp -R assets dist/assets",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  }
}
