name = "voltzware-landing"
compatibility_date = "2025-08-11"

# Static site via Workers + Assets (Workers Builds / wrangler deploy)
# Docs: https://developers.cloudflare.com/workers/ci-cd/builds/configuration/
[assets]
directory = "./dist"
